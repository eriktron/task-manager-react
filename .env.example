# .env.example

# URL de conexión a la base de datos de PostgreSQL
DATABASE_URL=postgresql://appuser:apppass@postgres:5432/appdb

# Puerto en el que corre el backend (por ejemplo, 3000 o 4000)
PORT=3000